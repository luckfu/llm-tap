请你基于 proxy_getway.py 的报文解析入库功能，编写一个新proxy_server的程序，这个程序提供统一的llm模型代理服务，代理服务提供自己的 auth tokens验证，再提取客户端访问的model 映射到 config.json配置中的api的endpoint,使用配置文件中的key，调用模型服务并返回数据
- 配置json，类似这样配置调用
```json
{
    "111": {
        "model": "Pro/deepseek-ai/DeepSeek-R1",
        "key": "sk-bmutyiebgsnrujeozzmwjbawwuxodmxfazethbdvfkturovv",
        "end_point": "http://api.siliconflow.cn/v1/chat/completions"
    },
    "222": {
        "model": "DeepSeek-R1",
        "key": "sk-abc123def456ghi789jkl012mno345pq",
        "end_point": "http://36.141.21.137:9081/v1/chat/completions"
    }
    ...
}
```
加入proxy_server提供的auth tokens
- 客户端可以通过下面的方式访问实际的模型服务： 
```bash
curl -X POST 'http://127.0.0.1:8080/v1/chat/completions' \
-H 'Content-Type: application/json' \
-H "Authorization: Bearer sk-？？？？" \
-d '{
    "model": "111",
    "messages": [
        {"role": "user", "content": "你是谁"}
    ],
    "temperature": 0,
    "stream": true
}'
```
- 完成proxy_getway.py程序中的数据解析入库功能，记住，如果客户端使用流式，返回也要流式





curl http://81.70.198.254:8080/v1/embeddings \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 72716f6e-e6f2-44e0-a374-1f1b1feb97a8" \
  -d '{
  "model": "ROOT-bge-m3",
  "input": [
   "(EM) 85.4 87.9 90.9 89.9 - 92.8 C-Eval (EM)"
  ],
  "dimensions": 1024
}'

如果把input去掉括号，改成 "EM 85.4 87.9 90.9 89.9 - 92.8 C-Eval EM" 就可以正常返回，这可能是什么原因？